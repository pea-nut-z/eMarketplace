// import * as actions from "./actionTypes";

// export const userAdded = (accountId) => (dispatch, getState, {getFirebase, getFireStore}) => {
//     const firestore = getFireStore()
//     firestore.collection('accounts').add({

//     })

//     console.log({ accountId });

//   dispatch({ type: actions.USER_ADDED, accountId, userId });
// };

// // dispatch({ type: actions.USER_ADDED, userId: id, username, location: "N/A" }
